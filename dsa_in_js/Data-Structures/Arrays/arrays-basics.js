// Arrays declaration
let arr = [1, 2, 3, 4, 5]; 
let arr2 = new Array(1, 2, 3, 4, 5); 
let arr3 = new Array(5); // creates an array of length 5
let arr4 = new Array(); // creates an empty array

// Accessing elements
console.log(arr[0]); // 1
console.log(arr[1]); // 2
console.log(arr[arr.length - 1]); // 5